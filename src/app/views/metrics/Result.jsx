import React from 'react'

const Result = ({ name, value }) => (
    <div>
        <div>
            <p color="red">{name}</p>
            <p color="blue">{value}</p>
        </div>
    </div>
);
export default Result
